"use strict";
function calculaArea(base, altura) {
    return base * altura;
}
const caculaArea2 = (base, altura) => base * altura;
//void
function somar(...numeros) {
    // numeros.reduce((total, num) => total + num);
    console.log(numeros);
}
function teste() {
    if (10 > 5) {
        return "dez maior q cinco";
    }
    else {
        return 5;
    }
}
const resultadoTeste = teste();
